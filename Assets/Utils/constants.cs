public enum SlotState
{
    WHITE,
    RED,
    EMPTY
}

public enum Line
{
    HORIZONTAL,
    FIRST_DIAGONAL,
    SECOND_DIAGONAL,
    VERTICAL
    
}

public enum Direction
{
    FORWARD = 1,
    BACKWARDS = -1
}